name: secret-guard

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  secret-guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Aftelle secret guard
        run: bash tools/secret_guard/check.sh
